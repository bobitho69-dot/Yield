# Yield AI fine-tuning (LoRA). Install on the GPU box:  pip install -r requirements.txt
# Versions are lower-bounds known to work together; newer usually fine.
torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.33.0
bitsandbytes>=0.43.0          # 4-bit (QLoRA) so a big model fits a small GPU
trl>=0.9.6                    # SFTTrainer — chat-format supervised fine-tuning
sentencepiece>=0.2.0
